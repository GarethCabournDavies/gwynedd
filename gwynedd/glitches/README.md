## Glitches as defined in Gwynedd

Each glitch has its own glitch_name.py module, and its definition is given in that module.

The inject.py module defines how to inject these glitches into the strain data.
